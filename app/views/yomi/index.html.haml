%h1 漢字を入力して下さい
%input#kanji{ type: "text" }
%input#convert{ type: "submit", value: "読みを教えて！"}
%p#yomi ここに読みを出力します

:javascript
  $('#convert').click(function() {
    $.post('yomi/convert', { q: $('#kanji').val() }, function(data) {
      $('#yomi').text(data);
    });
  });
